{% form_theme form 'bootstrap_5_layout.html.twig' %}
{{ form_start(form) }}
  <div class="space-y-4">
    {{ form_row(form.event) }}
    {{ form_row(form.reference) }}
    {{ form_row(form.player) }}
    {{ form_row(form.gridText) }}
  </div>
  <div class="mt-4">
    <button class="px-4 py-2 bg-blue-600 text-white rounded">Enregistrer</button>
    <a href="{{ path('admin_card_index') }}" class="ml-2 px-4 py-2 bg-gray-200 rounded">Annuler</a>
  </div>
{{ form_end(form) }}
