{% extends 'admin/_layout.html.twig' %}

{% block title %}Éditer joueur #{{ player.id }}{% endblock %}

{% block admin_body %}
<div class="bg-white rounded shadow p-6">
  <div class="flex items-center justify-between mb-4">
    <h1 class="text-2xl font-bold">Éditer joueur</h1>
    <a href="{{ path('admin_player_index') }}" class="px-3 py-1 bg-gray-200 rounded">Retour</a>
  </div>
  {% include 'admin/player/_form.html.twig' with { form: form } %}
</div>
{% endblock %}
